<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="A tool for FAST students and professors to request IT equipment.">
    <meta name="author" content="Samira Parveen">
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
    <style>
        /* Custom Styles */
        html, body {
            height: 100%;
            font-family: 'Roboto', sans-serif;
        }

        body {
            display: flex;
            flex-direction: column;
            background-color: #f8f9fa;
        }

        /* Ensure the footer stays at the bottom */
        .content {
            flex: 1;
        }

        .navbar {
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            background-color: #ffffff; /* or any other color */
        }

        .navbar-brand {
            font-weight: bold;
            color: #0d6efd;
        }

        .navbar-nav .nav-link {
            color: #555;
            margin-right: 15px;
            transition: color 0.3s ease;
        }

        .navbar-nav .nav-link:hover {
            background-color: rgba(13, 110, 253, 0.1);
            border-radius: 5px;
        }

        footer {
            padding: 20px 0;
            background-color: #343a40;
            color: #ffffff;
            text-align: center;
            margin-top: auto; /* Pushes the footer to the bottom */
        }

    </style>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-light">
            <div class="container-fluid">
                <a class="navbar-brand" href="#">FAST Equipment Tool</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Home" asp-action="Index" title="Go to the Home page">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Home" asp-action="AllEquipment" title="View all available equipment">All Equipment Listing</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Home" asp-action="AvailableEquipment" title="Check equipment availability">Available Equipment</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Home" asp-action="RequestForm" title="Submit a request for equipment">Request Form</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Home" asp-action="Requests" title="View admin requests">Admin Requests</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <main class="container content">
        @RenderBody()
    </main>

    <footer>
        <div class="container">
            <p>&copy; 2024 FAST Equipment Tool. All rights reserved. By: Samira Parveen </p>
        </div>
    </footer>

    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
